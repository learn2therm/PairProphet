name: validprot
channels:
  - defaults
  - conda-forge
  - plotly
dependencies:
  - biopython
  - autopep8
  - numpy
  - pandas
  - python
  - pip
  - scikit-learn
  - python-kaleido
  - joblib
  - pip:
      - duckdb
      - sphinx
      - sphinx-rtd-theme
      - pyhmmer
      - pylint
