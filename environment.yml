name: validprot
channels:
  - defaults
  - conda-forge
  - plotly
dependencies:
  - biopython
  - numpy
  - pandas
  - python
  - pip
  - scikit-learn
  - python-kaleido
  - joblib
  - pip:
      - duckdb
      - sphinx
      - sphinx-rtd-theme
      - pyhmmer
