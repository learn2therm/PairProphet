name: validprot
channels:
  - defaults
  - conda-forge
  - plotly
dependencies:
  - biopython
  - numpy
  - pandas
  - python
  - pip
  - scikit-learn
  - python-kaleido
  - joblib
  - pip:
      - duckdb==0.6.1
      - sphinx
      - sphinx-rtd-theme
      - pyhmmer==0.7.4
      - pypdb==2.2
