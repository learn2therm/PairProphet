name: pairpro
channels:
  - defaults
  - conda-forge
dependencies:
  - biopython
  - autopep8
  - nest-asyncio
  - numpy
  - pandas
  - python
  - pip
  - ray-default
  - scikit-learn
  - joblib
  - sphinx
  - dask
  - pip:
      - click
      - duckdb==0.7.1
      - httpx
      - sphinx
      - sphinx-rtd-theme
      - tqdm
      - pyhmmer
      - pylint
      - rdkit
      - iFeatureOmegaCLI
