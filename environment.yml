name: validprot
channels:
  - defaults
  - conda-forge
  - plotly
dependencies:
  - biopython
  - numpy
  - pandas
  - python
  - pip
  - scikit-learn
  - python-kaleido
  - joblib
  - pip:
      - duckdb==0.6.1
      - pyhmmer==0.7.3
      - pypdb==2.2
